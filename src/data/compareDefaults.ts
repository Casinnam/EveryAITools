/** Shown when a visitor has not picked tools yet, so /compare always renders a useful table. */
export const DEFAULT_COMPARE_IDS = ['chatgpt', 'claude', 'gemini'];

/** Most-searched tools surfaced in the static quick-reference table on /compare. */
export const POPULAR_TOOL_IDS = ['chatgpt', 'claude', 'gemini', 'perplexity', 'midjourney', 'canva', 'runway', 'elevenlabs'];
